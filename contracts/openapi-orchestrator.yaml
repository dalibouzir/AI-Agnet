openapi: 3.1.0
info:
  title: Orchestrator API
  version: 0.1.0
  description: Stub contract for the orchestrator routing service.
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                example:
                  status: ok
